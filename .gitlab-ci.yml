stages:
  - install
  - test

before_script:
  - python --version
  - pip install virtualenv
  - virtualenv venv
  - source venv/bin/activate

install_dependencies:
  stage: install
  script:
    - pip install -r requirements.txt
    - pip install pytest

test:
  stage: test
  script:
    - python -m pytest
